name: PR Build

on:
  pull_request:
    branches:
      - main
  workflow_dispatch: {}

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up dotnet
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.x.x'

      - name: Build
        run: |
          dotnet restore
          dotnet build --configuration Release --no-restore

      - name: Test
        run: |
          pwd
          dotnet test --configuration Release --no-build --verbosity normal \
              /p:CollectCoverage=true \
              /p:CoverletOutputFormat=opencover \
              /p:CoverletOutput="${{ github.workspace }}/TestResults/Coverage/coverage.opencover.xml"

      - name: Generate markdown report
        run: |
          dotnet new tool-manifest
          dotnet tool install dotnet-reportgenerator-globaltool
          dotnet tool restore
          dotnet reportgenerator -reports:"${{ github.workspace }}/TestResults/Coverage/coverage.opencover.xml" -targetdir:"${{ github.workspace }}/TestResults/Coverage" -reporttypes:MarkdownSummary
        env:
          PATH: ${{ runner.tool_cache }}/dotnet:$PATH

      - name: Upload coverage output
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report-raw
          path: ./TestResults/Coverage/coverage.opencover.xml

      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report-md
          path: "${{ github.workspace }}/TestResults/Coverage"

  report-coverage:
    runs-on: ubuntu-latest
    needs: ci
    steps:
      - name: Add coverage to PR
        run: |
          gh pr comment ${{ github.event.pull_request.number }} --body "$(cat ./TestResults/Coverage/Summary.md)"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}